# 第二大腦設定指南

> 版本：v1.0（通用版，適用各家 AI agent）
> 更新日期：2026-07-22
> 前置：你的 AI agent 已知道 `<VAULT_PATH>`，並已通過至少一種讀寫測試。

---

## 這份指南會幫你做什麼？

把一個 Obsidian vault 整理成適合 AI agent 長期協作的「第二大腦」：

- 建立三層資料夾：輸入、消化、輸出
- 建立全域與 vault 內兩層規則檔
- 建立不依賴 Templater 的純 Markdown 模板
- 建立 `02-知識庫/index.md` 與 `02-知識庫/log.md`
- 設定 Web Clipper 或手動剪藏流程
- 建立每週知識重整 SOP
- 保留踩坑筆記，讓後面使用者避開同樣問題

本指南不綁定特定工具。Claude Code、Codex、opencode、Antigravity 或其他支援讀寫本機檔案的 agent 都適用。

> [!important]
> 本指南預設「補缺，不覆蓋」。
> 如果目標檔案已存在，agent 應先讀取原檔並詢問使用者是否合併、跳過或備份後覆寫。

---

## 先確認：你現在是哪一種狀態？

| 狀態 | 做法 |
|------|------|
| 已知道 vault 路徑且 MCP 可用 | 可直接照本指南執行 |
| 已知道 vault 路徑但只用資料夾授權 | 可執行，但要確認 agent 對 `<VAULT_PATH>` 有寫入權限 |
| 還不知道 vault 在哪 | 先搜尋 `.obsidian` 資料夾定位 `<VAULT_PATH>`，多個候選要讓使用者選 |
| 已有成熟 vault | 務必使用「補缺，不覆蓋」模式 |
| 全新空 vault | 可照本指南完整建立 |

本指南所有路徑都用：

```text
<VAULT_PATH>
```

請把它替換成使用者實際的 Obsidian vault 路徑。

---

## 階段〇：確認你的 agent 用哪一種規則檔

各家 agent 讀取「長期記憶／專案規則」的檔名與位置不同。開始之前先確認，後面所有步驟都用這兩個佔位符：

- `<AGENT_HOME>`：agent 的全域設定資料夾
- `<AGENT_RULES_FILE>`：agent 讀取的規則檔名

常見對照：

| Agent | `<AGENT_HOME>` | `<AGENT_RULES_FILE>` |
|-------|----------------|----------------------|
| Claude Code | `~/.claude/` | `CLAUDE.md` |
| Codex | `~/.codex/` | `AGENTS.md` |
| opencode | `~/.config/opencode/` | `AGENTS.md` |
| Antigravity 2.0 | `~/.gemini/` | `GEMINI.md`（同時也讀 `AGENTS.md`） |
| 其他 | 查該工具文件 | 查該工具文件 |

Windows 的 `~` 對應 `C:\Users\<使用者>\`。

兩點補充：

- **opencode 的全域規則是 `~/.config/opencode/AGENTS.md`，不是 `opencode.json`。** `opencode.json` 管 MCP、provider、權限等設定，規則要另外開一個 `AGENTS.md`。專案層與全域層的 `AGENTS.md` 是**串接**的（專案在前、全域在後），不是覆蓋。
- **Antigravity 2.0 的全域規則檔是 `~/.gemini/GEMINI.md`**，但它自 v1.20.5 起也會讀 `AGENTS.md`，所以 vault 根的 `AGENTS.md` 對它是有效的，不必額外做橋接檔。另外它的專案層規則放在 `.agents/rules/` 資料夾（舊版是 `.agent/rules/`），與本指南用的 vault 根規則檔是兩套機制，不要混用。

> [!tip]
> 不確定就直接問使用者，或請 agent 查自己的官方文件。**不要猜**——寫錯位置的規則檔完全不會被讀到，而且很難發現。

### 跨 agent 共用的做法

如果你會用不只一種 agent 讀同一個 vault，建議：

1. Vault 根目錄**只放一份** `AGENTS.md`（這是目前較多工具共用的檔名）作為唯一真實來源。
2. 若某個 agent 只讀別的檔名，就在 vault 根另外放一個極短的**橋接檔**，不重複規則內容，只把 `AGENTS.md` 帶進來。

若該 agent 支援 import 語法（例如 Claude Code 的 `@`），橋接檔第一行直接匯入：

```markdown
@AGENTS.md
```

若不支援 import，就退而求其次寫一句指路：

```markdown
本 vault 的筆記規則請讀取 `AGENTS.md`。
```

這樣規則只需要維護一份，不會出現兩份規則互相矛盾。詳細做法見階段三的 3-3。

> [!warning]
> 這一步很容易被跳過，因為**漏掉不會報錯**——只是那個 agent 安靜地不套用任何筆記規則，而其他 agent 一切正常。
> 只要你用了不只一種 agent，就把橋接檔當成必做項，不是選配。

---

## 階段一：詢問使用者偏好

Agent 先問使用者四件事：

1. 你的職業或主要身份是什麼？
2. 你的筆記主要用途是什麼？例如教學、研究、影片創作、專案管理。
3. 希望筆記與回覆使用什麼語言？預設繁體中文。
4. 這個 vault 是否已經有重要資料？如果有，預設不覆蓋任何既有筆記。

建議記錄成：

```markdown
## 使用者偏好

- 身份：<使用者回答>
- 主要用途：<使用者回答>
- 語言：繁體中文
- 覆寫原則：不覆寫既有筆記，先讀原檔再詢問
```

---

## 階段二：建立三層資料夾結構

在 `<VAULT_PATH>` 建立下列資料夾。已存在就跳過。

```text
<VAULT_PATH>/
├── 00-每日筆記/         # 時間管理：每日紀錄、週計畫、週報
├── 01-Clippings/        # 輸入：網路文章、影片筆記、外部資料
├── 02-知識庫/           # 消化：AI 整理後的結構化知識
├── 03-創作庫/           # 輸出：自己的教案、教材、腳本、報告
├── 99-Templates/        # 純 Markdown 模板
├── AGENTS.md            # vault 內筆記規則（唯一真實來源）
└── <其他規則檔名>       # 橋接檔，只在你的 agent 不讀 AGENTS.md 時才需要（見階段三 3-3）
```

### 為什麼要加數字前綴？

Obsidian 側邊欄依字母序排列。若不加前綴，`Clippings`、`Templates` 會落在英文區，中文資料夾落在中文區，兩群中間夾著 vault 裡其他所有資料夾，很難一眼找到。

數字在排序上優先於所有字母與中文，加了前綴這五個資料夾就會**固定黏在 vault 最頂端**，日後新增再多資料夾也不會被插隊。編號同時帶有語意：

- `00` 每日筆記：開啟頻率最高，放最上面
- `01` → `02` → `03`：知識流動的方向（輸入 → 消化 → 輸出）
- `99` Templates：基礎設施，平常不需要打開，沉到最底

中間的 `04`–`98` 留白，供日後擴充。

> [!tip]
> 如果是替既有 vault 改名，請**在 Obsidian 應用程式內重新命名**，不要用檔案總管。
> 在 app 內改，Obsidian 會自動更新所有受影響的連結與查詢；在檔案總管改，連結會斷掉。

Windows（PowerShell）：

```powershell
$vault = "<VAULT_PATH>"
$folders = @("00-每日筆記", "01-Clippings", "02-知識庫", "03-創作庫", "99-Templates")
foreach ($folder in $folders) {
  New-Item -ItemType Directory -Force -Path (Join-Path $vault $folder) | Out-Null
}
```

macOS / Linux（bash）：

```bash
vault="<VAULT_PATH>"
for folder in 00-每日筆記 01-Clippings 02-知識庫 03-創作庫 99-Templates; do
  mkdir -p "$vault/$folder"
done
```

資料夾語意：

| 層 | 資料夾 | 放什麼 | 原則 |
|----|--------|--------|------|
| 時間 | `00-每日筆記/` | 每日、每週、重整週報 | 記錄流程 |
| 輸入 | `01-Clippings/` | 剪藏、逐字稿、外部資料 | 原始資料，少改寫 |
| 消化 | `02-知識庫/` | AI 整理後的主題頁 | AI 寫，人審 |
| 輸出 | `03-創作庫/` | 自己的作品 | 保留個人聲音 |
| 模板 | `99-Templates/` | 筆記格式 | 不依賴外掛 |

---

## 階段三：建立兩層規則檔

### 3-1. 全域規則檔：讓 agent 記得 vault 在哪

位置：

```text
<AGENT_HOME>/<AGENT_RULES_FILE>
```

建議加入：

```markdown
## Obsidian 第二大腦

主要 Obsidian Vault：

`<VAULT_PATH>`

當我說「Obsidian」、「Secondbrain」、「我的筆記本」、「第二大腦」時，預設指這個資料夾。

如果任務涉及筆記、剪藏、知識庫、創作庫、每日筆記、專案工作流程，請優先讀取：

- `<VAULT_PATH>/AGENTS.md`
- `<VAULT_PATH>/02-知識庫/index.md`
- `<VAULT_PATH>/02-知識庫/log.md`

實際讀寫仍以 agent 的工作區授權或 MCP 設定為準。
```

> [!warning]
> 修改全域規則檔前，先讀取原檔。這個檔案通常已經有其他專案的規則，只能追加，不能覆寫。

### 3-2. Vault 根規則檔：定義筆記規則

位置：

```text
<VAULT_PATH>/AGENTS.md
```

建議內容：

```markdown
# 我的第二大腦 — 筆記規則

## 使用者身份

- 身份：<使用者身份>
- 主要用途：<教學 / 研究 / 創作 / 專案管理>
- 語言：繁體中文

## 資料夾規則

| 資料夾 | 用途 | 原則 |
|--------|------|------|
| `00-每日筆記/` | 時間紀錄 | 記錄每日、每週與重整結果 |
| `01-Clippings/` | 外部輸入 | 原始資料，不任意改寫 |
| `02-知識庫/` | 結構化知識 | AI 可協助整理，使用者審核 |
| `03-創作庫/` | 自己的作品 | 不改寫個人聲音，除非使用者要求 |
| `99-Templates/` | 模板 | 純 Markdown，避免外掛依賴 |

## 新增筆記規則

- 正式筆記要加 frontmatter：`title`、`date`、`type`、`tags`
- 優先用 Obsidian 雙向連結 `[[...]]`
- 新增知識庫頁面後，要更新 `02-知識庫/index.md`
- 重要操作要追加到 `02-知識庫/log.md`

## 安全規則

- 不覆寫既有筆記；如果檔案存在，先讀原檔再詢問
- 不刪除筆記，除非使用者明確指定
- 不批次改寫個人風格強烈的文章
- 不把 API Key、token、密碼寫進筆記

## 常用指令語意

- 「新增到筆記」：存進本 vault，並自動判斷資料夾
- 「整理成知識庫」：從 `01-Clippings/` 或輸入內容建立 `02-知識庫/` 頁面
- 「跑一次知識重整」：執行每週知識重整 SOP
```

> [!tip]
> 全域規則檔管「agent 要去哪裡找 vault」。
> Vault 根規則檔管「這個 vault 裡的筆記怎麼寫」。
> 兩者不要混在一起——vault 的筆記規則寫進全域檔，會污染所有不相干的專案。

### 3-3. Vault 根橋接檔：讓不讀 `AGENTS.md` 的 agent 也吃得到規則

**只有當你的 `<AGENT_RULES_FILE>` 不是 `AGENTS.md` 時才需要這一步。**回頭看階段〇的對照表：如果那一欄寫的是 `CLAUDE.md`、`GEMINI.md` 或其他檔名，那麼 3-2 建的 `AGENTS.md` 對這個 agent 是完全隱形的。

位置：

```text
<VAULT_PATH>/<AGENT_RULES_FILE>
```

內容（以 Claude Code 為例，檔名 `CLAUDE.md`）：

```markdown
@AGENTS.md

<!--
  本檔是「橋接檔」：Claude Code 只讀 CLAUDE.md，不讀 AGENTS.md，
  所以用第一行的 @AGENTS.md 把 vault 筆記規則 import 進來。
  筆記規則一律寫進 AGENTS.md（唯一真實來源），這裡只放 Claude Code 專屬規範，避免兩份分叉。
-->

## <Agent 名稱> 專屬

<!-- 只有這個 agent 才需要的規範；沒有就留白 -->
```

不支援 import 語法的 agent，第一行改寫成一句指路即可：

```markdown
本 vault 的筆記規則請讀取 `AGENTS.md`，以該檔內容為準。
```

三個原則：

- **橋接檔不放規則。** 只放 import／指路，加上該 agent 專屬的少數規範。一旦開始複製貼上規則內容，兩份就會慢慢分叉。
- **每個檔名各一份。** 用三種 agent、三種不同檔名，就放三個橋接檔，全部指向同一份 `AGENTS.md`。
- **建完要驗。** 在 vault 目錄開該 agent，問它「這個 vault 的筆記規則是什麼」。答得出 frontmatter 欄位或資料夾原則才算生效——沒生效不會有任何錯誤訊息。

> [!note]
> 橋接檔放在 vault 根目錄，在 Obsidian 側邊欄會被當成一則筆記顯示（`AGENTS.md` 也一樣）。這是正常的，不影響運作；不想看到可以用 Obsidian 的排除設定隱藏。

---

## 階段四：建立純 Markdown 模板

本指南預設不用 Obsidian Templater，避免外掛依賴與 `<% tp.xxx %>` 被誤展開。

在 `99-Templates/` 建立三個模板。若檔案已存在，先讀原檔再詢問是否合併。

模板中的 `YYYY-MM-DD`、`YYYY-WXX` 是留給使用時替換的佔位符，不要在建立模板時就填成今天的日期。

### 4-1. `99-Templates/每日筆記.md`

```markdown
---
title: 每日筆記
date: YYYY-MM-DD
type: daily-note
tags: [每日筆記]
---

# YYYY-MM-DD 每日筆記

## 今日重點

- （待填）

## 教學 / 工作紀錄

- （待填）

## 收集到的材料

- （待填）

## 需要整理進知識庫

- （待填）

## 明日優先事項

1. （待填）
2. （待填）
3. （待填）
```

### 4-2. `99-Templates/週計畫.md`

```markdown
---
title: 週計畫
week: YYYY-WXX
type: weekly-plan
tags: [週計畫]
---

# YYYY-WXX 週計畫

## 本週目標

- （待填）

## 本週重要事件

| 日期 | 事件 | 備註 |
|------|------|------|
| | | |

## 本週創作 / 專案

- （待填）

## 本週要整理的 Clippings

- （待填）

## 週末回顧

- 做得好的事：
- 卡住的事：
- 下週調整：
```

### 4-3. `99-Templates/知識庫頁面.md`

```markdown
---
title: 知識庫頁面
type: knowledge
date: YYYY-MM-DD
tags: []
source:
---

# 知識庫頁面標題

## 核心概念

用一段話說明這篇筆記在解決什麼問題。

## 重點整理

- （待填）

## 可應用場景

- （待填）

## 與既有筆記的連結

- [[相關筆記]]

## 內容缺口

- （待填）
```

### 4-4. Templater 進階選項

如果使用者熟悉 Obsidian Templater，可以另外建立 Templater 版本模板。

> [!warning]
> 不要把含 `<% tp.xxx %>` 的示範語法放在會被 Templater 自動執行的資料夾。
> 曾遇過教學文件中的 `<% %>` 被誤展開，導致範例不可逆地變成日期或具體文字。
> 預設建議先用純 Markdown 模板。

---

## 階段五：建立知識庫初始檔案

### 5-1. `02-知識庫/index.md`

若不存在，建立：

```markdown
---
title: 知識庫索引
type: index
updated: YYYY-MM-DD
tags: [知識庫]
---

# 知識庫索引

> 最後更新：YYYY-MM-DD

## 主題總覽

| 主題 | 一行摘要 | 相關筆記 |
|------|----------|----------|
| （等待第一次整理） | | |

## 最近新增

| 日期 | 筆記 | 來源 |
|------|------|------|
| | | |

## 待補缺口

- （待填）
```

### 5-2. `02-知識庫/log.md`

若不存在，建立：

```markdown
---
title: 知識庫操作紀錄
type: log
tags: [知識庫, log]
---

# 知識庫操作紀錄

## YYYY-MM-DD 初始化

**操作類型：** 初始化

**執行工具：** <agent 名稱>

**建立內容：**

- `00-每日筆記/`
- `01-Clippings/`
- `02-知識庫/`
- `03-創作庫/`
- `99-Templates/`
- `AGENTS.md`

**備註：**

- 預設不覆寫既有筆記。
```

> [!tip]
> log 記錄「執行工具」是為了多 agent 共用時能追溯：哪一次整理是誰做的、風格為什麼不一致。

---

## 階段六：設定剪藏流程

### 6-1. Web Clipper（可選）

如果使用者有安裝 Obsidian Web Clipper：

1. 點瀏覽器工具列上的 Web Clipper 圖示
2. 點面板右上角的**齒輪**圖示進入設定
3. 左側選 **Templates**，點選使用中的範本（通常是 **Default**）
4. 確認 **Vault** 欄位指向目標 vault
5. 找到 **Note location** 欄位（部分版本標示為 Folder 或 Path），設成 `01-Clippings`
6. 設定自動儲存，關閉即可
7. 測試剪一篇文章，確認檔案出現在 `01-Clippings/`

> [!important]
> **儲存位置是綁在「範本」上的，不是全域設定。**
> 這是最容易找錯的地方——很多人在設定頁面翻半天，因為它根本不在全域那一層。
>
> 如果建過不只一個範本（例如 YouTube、論文各一個），**每個範本都要各改一次**，它們的 Note location 互相獨立。

> [!tip]
> 判斷有沒有漏改：剪藏後如果 vault 根目錄自己長出一個沒有編號前綴的 `Clippings/`，就代表還有某個範本指向舊路徑。

### 6-2. 沒有 Web Clipper 的替代做法

沒有 Web Clipper 也沒關係。

使用者可以直接對 agent 說：

> 幫我把這篇文章存進 Obsidian 的 Clippings，標題是「XXX」，來源網址是「YYY」。

建議存成：

```markdown
---
title: <文章標題>
source: <網址>
date: YYYY-MM-DD
type: clipping
tags: [Clippings]
---

# <文章標題>

## 原始摘要

## 原文重點

## 我想保留的原因
```

---

## 階段七：每週知識重整 SOP

不論用哪一種 agent，最穩的導入順序都是：

1. 先用手動方式跑一次，確認流程正確。
2. 確認你的 agent 有沒有內建排程／自動化功能；有就用它建立每週任務。
3. 若沒有內建功能或自動化失敗，回到手動觸發，並用規則檔提醒當備援。

> [!important]
> 先手動驗證再自動化。沒驗證就排程，等於讓 agent 每週對你的筆記做一件你沒檢查過的事。

### 7-1. 先手動跑一次（必要驗證）

每週日對 agent 說：

```text
請跑一次我的 Obsidian 每週知識重整。
請依照 vault 根目錄 AGENTS.md 的規則執行。
不要覆寫既有筆記；若需要更新既有頁面，先摘要你要改哪裡再詢問。
```

手動成功後，確認：

- Agent 能讀取 vault
- Agent 能建立週報筆記
- `02-知識庫/index.md` 與 `02-知識庫/log.md` 的更新規則合理
- 沒有覆寫既有重要筆記

### 7-2. 建立定期自動化（推薦）

手動驗證成功後，先確認你的 agent 是否有內建排程功能（各家名稱不同：排程任務、自動化、routines、scheduled tasks…）。有的話，對 agent 說：

```text
請幫我建立一個每週日早上 9:17 執行的定期任務。

任務名稱：Obsidian 每週知識重整

工作區：<VAULT_PATH>

任務內容：
請執行 Obsidian 筆記庫的每週知識重整。
請依照 vault 根目錄 AGENTS.md 的規則執行。
不要覆寫既有筆記。
若需要更新既有頁面，先整理更新計畫。
完成後建立週報到 `00-每日筆記/`，並更新 `02-知識庫/index.md` 與 `02-知識庫/log.md`。
最後回報新增與修改的檔案清單。
```

優先使用 agent 自己的排程功能，而不是叫使用者去寫 cron 或 Windows 工作排程器——前者對一般使用者友善得多，也比較容易停用與除錯。

### 7-3. 規則檔主動提醒（備援）

如果你的 agent 沒有內建排程，可以在全域規則檔加：

```markdown
## 知識重整提醒

如果今天是週日，且本週尚未做過知識重整，請在對話開始時提醒使用者：
「今天要不要跑一次 Obsidian 每週知識重整？」

只有在使用者同意後才執行。
```

這不是自動化，只是提醒規則——你要先開啟對話它才會觸發。真正的定期執行仍請用排程功能。

### 7-4. 重整 prompt

```text
請執行 Obsidian 筆記庫的每週知識重整。

前提：
- 使用 vault 根目錄 AGENTS.md 的規則
- 不覆寫既有筆記
- 新增或修改前先說明計畫

第一步：盤點本週變動
- 找過去 7 天新增或修改的筆記
- 分成 Clippings、創作庫、知識庫、每日筆記

第二步：整理 Clippings
- 讀本週新增 Clippings
- 分析主題與關聯
- 為每個主題建立或更新知識庫頁面
- 沒有新 Clippings 就跳過

第三步：創作庫回流
- 讀本週創作庫變動
- 若有可沉澱的概念，整理到知識庫

第四步：知識庫健康檢查
- 檢查孤兒頁面、缺少摘要、缺少來源、缺少雙向連結

第五步：路徑與用詞健康檢查
- 讀取 vault 根目錄 AGENTS.md，確認裡面提到的每個資料夾都真的存在
- 讀取本機各 agent 的全域規則檔，確認裡面列出的每個路徑都真的存在
- 檢查是否有已淘汰的名詞仍出現在現行敘述中（標題、frontmatter、規則、操作指示）
- 判斷依據：檔名或資料夾名已改用新稱呼，但內文仍在用舊稱呼
- 歷史紀錄與日誌中的舊名詞屬於當時事實，不列入
- 若發現失效路徑或過時用詞，列出來並詢問要修正還是保留
- 不要自行修改全域規則檔，先報告再等指示

第六步：更新索引與 log
- 更新 `02-知識庫/index.md`
- 在 `02-知識庫/log.md` 追加本次操作紀錄

第七步：產出週報
- 建立 `00-每日筆記/YYYY-WXX 知識重整.md`
- 摘要本週新增知識、可用素材、待補缺口
- 若第五步有發現失效路徑或過時用詞，一併記錄

完成後回報新增與修改的檔案清單。
```

這段 prompt 不含任何工具專屬語法，可以直接貼給任何 agent 使用。

> [!important]
> **第五步是為了對抗「安靜失效」。**
> 規則檔裡的路徑失效不會有任何錯誤訊息——agent 讀不到就跳過，你完全不會發現。
> 資料夾改名、筆記搬家、專案命名慣例改變，都會留下這種指向空氣的路徑。
> 這種問題只能靠定期主動檢查抓出來。把它綁進每週本來就要跑的流程，比寫一條「記得偶爾檢查」的提醒可靠得多——後者不會有人執行。

> [!tip]
> **改名這件事，最常只做一半。**
> 典型情況是檔名與資料夾改好了，內文的標題、frontmatter、操作指示卻還留著舊稱呼——因為前者是機械操作，後者要逐句判讀。
> 半年後回頭看，同一個東西在筆記裡有兩三種叫法，而且分不出哪個才是現行的。
>
> 用詞檢查跟路徑檢查放在同一步，是因為它們的成因相同：**都是「改了一部分」留下的殘跡**。
> 但兩者的處理原則不同——失效路徑該修，歷史紀錄裡的舊名詞不該動，那是當時的事實。

### 7-5. 系統排程是什麼？為什麼不當主流程？

系統排程是指：

- 用 macOS / Linux 的 `cron`，或 Windows 工作排程器
- 在固定時間呼叫 agent 的 CLI 指令
- 讓作業系統定期啟動 agent

這條路線需要處理 CLI 參數、環境變數、權限與非互動模式，只有進階使用者需要。

> [!warning]
> 只有在使用者明確說「我要用終端機 / CLI / 系統排程」時，才補充系統排程。
> 本指南預設請使用 agent 內建的排程功能，或手動觸發。

---

## 階段八：歡迎筆記與驗證

### 8-1. 建立歡迎筆記

若不存在，建立：

```markdown
---
title: 歡迎來到你的第二大腦
type: guide
date: YYYY-MM-DD
tags: [Secondbrain, guide]
---

# 歡迎來到你的第二大腦

這個 Obsidian vault 用來收集、整理、沉澱與輸出你的知識。

## 你的資料夾

| 資料夾 | 用途 |
|--------|------|
| `00-每日筆記/` | 每日與每週紀錄 |
| `01-Clippings/` | 收集外部材料 |
| `02-知識庫/` | 整理後的知識 |
| `03-創作庫/` | 自己的作品 |
| `99-Templates/` | 筆記模板 |

## 你平常只要做兩件事

1. 把值得保留的資料放進 `01-Clippings/`
2. 每週跑一次知識重整

## 可以對你的 AI agent 說

- 搜尋我的筆記有沒有跟某主題相關的內容
- 幫我把這篇文章存進 Clippings
- 把這批 Clippings 整理成知識庫頁面
- 跑一次每週知識重整
```

### 8-2. 驗證清單

- [ ] `<VAULT_PATH>` 已確認
- [ ] `<AGENT_HOME>` 與 `<AGENT_RULES_FILE>` 已確認（不是用猜的）
- [ ] `00-每日筆記/`、`01-Clippings/`、`02-知識庫/`、`03-創作庫/`、`99-Templates/` 已存在
- [ ] 全域規則檔已記錄 vault 路徑，且原有內容未被覆寫
- [ ] 全域規則檔確實存在於該 agent 會讀的位置（用 `ls` 或檔案總管親眼確認，不要憑印象）
- [ ] `<VAULT_PATH>/AGENTS.md` 已建立
- [ ] 若 `<AGENT_RULES_FILE>` 不是 `AGENTS.md`，`<VAULT_PATH>/<AGENT_RULES_FILE>` 橋接檔已建立
- [ ] 已在 vault 目錄問過該 agent「這個 vault 的筆記規則是什麼」，確認它答得出來
- [ ] 三個純 Markdown 模板已建立
- [ ] `02-知識庫/index.md` 與 `02-知識庫/log.md` 已建立
- [ ] 歡迎筆記已建立
- [ ] 已完成一次讀寫測試
- [ ] 已手動跑過一次知識重整
- [ ] 若需要定期執行，已建立排程任務

---

## 如果失敗，照這個順序檢查

1. `<VAULT_PATH>` 是否正確
2. Agent 是否有 vault 寫入權限，或 Obsidian MCP 是否已載入
3. 全域規則檔是否放在該 agent 真正會讀的位置與檔名
4. 該 agent 是否讀得到 vault 根規則檔——若它的 `<AGENT_RULES_FILE>` 不是 `AGENTS.md`，橋接檔建了嗎（見 3-3）
5. 目標檔案是否已存在
6. 是否不小心用 Templater 執行了教學範例
7. 是否把全域規則檔與 vault 根規則檔混在一起

---

## 實測紀錄與踩坑筆記

### 踩坑 1：不要用「參考某某版」代替完整內容

對外文件應該可以獨立執行。若寫「內容跟某某版相同」，使用者會被迫跳到另一份文件，而且很容易把不同工具的規則檔名、技能機制、排程概念混在一起。

解法：一份文件內建完整模板與完整步驟；工具差異用佔位符與對照表處理，不用交叉參照。

### 踩坑 2：Templater 不應作為預設

Templater 是 Obsidian 外掛，不是每個使用者都有裝。含 `<% tp.xxx %>` 的教學文件也可能被誤執行。

解法：預設純 Markdown；Templater 只放進階選項。

### 踩坑 3：不要把系統排程當成 agent 內建自動化

系統排程是用作業系統去呼叫 CLI，例如 cron 或 Windows 工作排程器。這跟 agent 內建的排程功能不是同一件事，設定方式與失敗模式都不同。

解法：以 agent 內建排程為推薦路線；手動觸發作為驗證與備援；系統排程只放進階說明。

### 踩坑 4：不要假設 Google Drive 路徑

使用者可能用 OneDrive、Obsidian Sync、iCloud、本機資料夾。

解法：全篇使用 `<VAULT_PATH>`，執行前先請使用者確認實際路徑。

### 踩坑 5：不要猜規則檔的檔名與位置

`CLAUDE.md`、`AGENTS.md`、`GEMINI.md` 放錯位置都不會報錯，只是安靜地不生效，事後很難察覺。

解法：見階段〇，先確認再寫；不確定就問使用者或查官方文件。

### 踩坑 6：多個 agent 共用同一個 vault 時，規則要單一來源

兩份規則檔各寫各的，遲早互相矛盾——一個 agent 說要加 frontmatter，另一個不加，筆記格式就散掉了。

解法：vault 根只維護一份規則檔，其他檔名一律做成橋接檔。

### 踩坑 7：橋接檔漏建不會報錯，只會安靜地少一個 agent

實測遇過：vault 根有 `AGENTS.md`，Codex 與 opencode 都正常吃到規則，唯獨 Claude Code 完全不套用——因為它只讀 `CLAUDE.md`，那份 `AGENTS.md` 對它等於不存在。整套設定看起來「有做完」，實際上少了一個 agent，而且沒有任何徵兆。

同一次還發現全域規則檔也不見了：交接紀錄寫「已建立」，實際上檔案不在。**紀錄寫了不等於檔案存在。**

解法：階段〇的對照表確認完 `<AGENT_RULES_FILE>` 後，凡是不等於 `AGENTS.md` 的，當場就把橋接檔列進待辦；驗收時親眼確認檔案存在，再實際問一次 agent，不要只信文件紀錄。

---

## 本指南不做的事

- 不強迫使用 Google Drive
- 不要求安裝 Templater
- 不把系統排程當作預設自動化方案
- 不覆寫既有筆記，也不覆寫既有的全域規則檔
- 不綁定特定 AI agent 的技能、外掛或指令設計

---

## 相關連結

- [AGENTS.md 規格說明（OpenAI Codex 文件）](https://developers.openai.com/codex/guides/agents-md)
- [Obsidian Web Clipper](https://obsidian.md/clipper)
